# Outputs of the corpus harness (regenerated locally; reference user-specific
# paths, hence not tracked).
probe.ndjson
probe.err
manifest.json
# Baseline screenshots are large binaries regenerated on demand for human review;
# the golden JSON dumps under baselines/ are the committed regression signal.
baselines/screenshots/
# Default: ignore any save directories under saves/ (personal save data
# shouldn't accidentally get committed). Specific tracked corpora opt in
# via !saves/<name>/ below.
saves/*/
!saves/steam_classic_2023/
!saves/native_port64/
