# Log

Newest first. One entry per change to the bundle. Findings that bear on what the profile should say are logged here too, because SCHEMA.md is a draft and the first slices exist to test it.

## 2026-09-10

**Sixth slice, transitions and the save file, both adjacent to the recorder.** Two Subsystems distilled from docs/TRANSITIONS.md and docs/SAVEGAME.md with every claim re-read in AMBIANCE.CPP, OBJECT.CPP, DISKFUNC.CPP, PLAYACF.CPP, PERSO.CPP, SAVEGAME.CPP and CONTROL.CPP at 9f3750f5: black brackets the load, with the fade-out inside the load and the fade-in at the next render's tail; and the save as three files with three writers, a load that is a cube change with the fade left out, and a clock carried and installed. One Format for the `.lba` file, the first at `partial`: the three wire structs, the bounds helpers, the thumbnail and the decoder are host-tested, the whole payload's round trip is not, and the corpus harness needs retail data. Four Decisions from docs/plan/SAVE_WIRE_PLAN.md's resolved four, each checked against what the tree does rather than what the plan said. Five Quirks, all original behaviour read against the initial import, commit 333929ab, which is the first use of the evidence row the schema had worded as "the disassembly": the video player leaves the screen black, a fade to black on a black palette is a no-op, `AffScene` presents before it reveals, two exterior cubes change without a fade when the horizon is drawn, and the letterbox slides only on an objects-only flip. The community rules of the family, a fade re-presents with `BoxBlit` and never `BoxUpdate`, a palette ramp must present, a page is flipped after its last draw, are contracts of the transitions Subsystem and not Quirks, by the rule applied five times before. The adjacency the slice was chosen for is now explicit: `FlagFade` and `FlagBlackPal` are carried header lines because no savegame holds them and a skipped fade pumps no clock, the `.rec` Format's snapshot chunk is a file of the `.lba` Format written under the player name `rec`, and the load's trailing timer bracket links the save Subsystem to the harness Decision and the counter Quirk. The schema's evidence row now names the initial import's C++ beside the disassembly.

**Peer corrections to the sixth slice, each re-derived from the tree before it went in.** Four concurrent sessions answered the status note, and all four contradicted one clause: the exterior Quirk said the detail level is not in a recording's header. It is, as `settings.DetailLevel`, the last line of the block `Control_DescribeState` writes; `mode_line_ignored` carries no `settings.` key, so it is compared and a difference is printed by name; and two peers read it out of real files, a contributed session at 2 and the in-tree menu fixture at 3. The correction inverts the clause and sharpens the row. The hazard is not that the file cannot say which regime it was in, it is that the level is carried and compared and never installed, so a contributed island walk replays on the operator's level, diverges for a reason the file recorded and the engine printed with the boot banner, and the `replay ended` line carries no count of mode differences; the Format's comparison paragraph now says so, and the `.rec` row for `settings.` now describes the block by its cfg keys rather than by a number, since the previous count was wrong too. The key's provenance came with the correction and is in the code: the settings list is empirical, one cfg key changed at a time on the replay side of a recorded session, and `DetailLevel` is there because 3 to 0 was measured to diverge at tick 235 through the rain's draws on the shared random stream, so the fade is a second mechanism on the same key. One peer measured the suppressing direction on `FollowCamera`: matching the recording's value revealed a camera-distance difference at tick 0 that the mismatched run had never computed; the same peer marks a mismatch producing a clean verdict on a run that would otherwise diverge as plausible and not demonstrated, so the row says suppress rather than hide. The verdict's silence on mode differences is a recorded choice, not a gap: the coverage gate's pull request names it a sibling defect on a different predicate and leaves it out for its own calibration, and the Format now says so with the citation. Two reports kept as reports: on one contributed verbose file the two fade flags take 224 transitions each, 112 fades, and every fade lasts exactly one tick, read from per-tick telemetry, while the keyframes, a 3% sample, had earlier read the same file as two transitions in ninety thousand ticks; it agrees with the transitions Subsystem's derived row and is one file. And a comment defect in RECORD.CPP, from the peer who wrote it: the justification for ignoring `mode.step_armed_tick` says "same 598 ticks" where the shipped fixture measures 98, which the `.rec` Format already carries as 98.

**Finding, the principle has an exception the reference doc does not name.** `LoadScene` and `ChangeCube` both gate the fade on `!FlagDrawHorizon OR CubeMode + LastCubeMode != 2`, so at every detail level but the lowest a walk between two exterior cubes fades nothing, clears nothing, stops no sample and presents the new cube over the old one lit. docs/TRANSITIONS.md states "black brackets the load" without it. The gate is in the initial import. A reader who takes the principle as universal adds a blackout to every outdoor step, and an oracle that expects a fade per cube change false-alarms on the island walk; the Quirk carries both directions. A doc fix is a change of its own.

**Finding, the plan says CI oracle and the tree runs it locally.** SAVE_WIRE_PLAN.md resolves decision 3 as "use the committed 50 as the CI oracle". The corpus README says the driver requires retail game data and does not run in public CI, and no workflow in `.github/workflows` names it; it is `make savegame-corpus`. The Decision says both, and which is true of which job. The same check on decision 4: the whole-file byte-identity contract for compressed saves is declared, and nothing in the tree compresses a corpus payload and compares, `Compress_LZSS` has no caller outside the engine, and the empirical report the plan's first phase promised has no trace beside it. The Decision carries a "Where it stands" section saying the contract is unasserted and what the first measurement is.

**Finding, the trial's blind spot changed sides.** The stride trial validates `IndexFile3D` per object, and the first object's copy sits in the 140-byte prefix that is the same at both strides, so a one-object file validates under both hypotheses. The wire-first reader's known blind spot was a one-object native file; the native-first reader's is a one-object wire file, which is to say a retail save from a cube holding only the hero. Derived from the validator and not measured: no save in either corpus has fewer than three objects. The Decision says so in those words, and the Format's negative space points at it.

**Fifth slice, the control harness and the console.** Two Subsystems distilled from docs/CONTROL.md and docs/CONSOLE.md with the contracts checked against CONTROL.CPP, CONSOLE.CPP and PERSO.CPP: the harness as an outside-in instrument whose whole plan is fixed before boot, with headless as the only supported automation mode and determinism a property of the run's configuration; the console as the one command bus behind four front-ends. One Format for the `--listen` socket's line protocol, from CONTROL_PROTO.H, whose three buffers are host-tested without a socket. Three Decisions: the socket is a transport that does not arm the harness and gives up reproducibility on purpose; console output is a parsed contract, appended to and never reformatted; one flag table drives validation and two-tier help. One Quirk, the first in this bundle that is original game behaviour met through the harness rather than the recorder: the opening scene's dialogue about four seconds in, with the release-wait on modal entry as the reason an early-armed key does not dismiss it. A Reference for the three host test directories. Two contracts that read as traps went into the console Subsystem rather than as Quirks, because they are community behaviour rather than the original game's: a bool cvar with no argument toggles, and the tokenizer has no quoting. The `--exec` race with a scene change and the two-layer input model are contracts of the harness for the same reason.

**Peer input to the fifth slice, each item re-derived from the tree before it went in.** Four concurrent sessions answered the status note. Taken: the full arming set, sixteen flags that set the harness active and eleven that do not, with `--replay` the surprising one, since a replay alone supplies no scene and refuses by name; `--headless` and audio being one switch, so no flag combination gives a windowless run with audio; the recorder-armed run as the carve-out from the windowed-audio warning, because both ends switch voice lifetime onto the game clock; the `--exec-at` cap of sixteen; the `settings.` header block the `.rec` Format had omitted, ten `FollowCam` keys written by the harness and compared but never installed, so an operator's auto-camera setting reads as a divergence at tick 0; the -1 a run that never armed writes into `mode.step_armed_tick`; and the harness re-entering `MainLoop` through the menu's own loop since #663, which the recorder Subsystem already carried and the control Subsystem now does. Two doc defects found on the way: the comment atop `tests/automation/test_exec.sh` and the list in docs/CONTROL.md both name `playvideo` as a verb that blocks a headless run, and it has not been one since the video fix, which the record-and-replay fixture demonstrates every run. And one filed defect against the parsed-output contract: #669, the `screenshot` verb printing a path it does not write when a frame never completes, which is the contract broken in the direction that matters, and which the Decision now carries as an open breach. Left out at first: a peer's two proposed recording invariants, every clock value in a pinned run a multiple of the step and every fade completing within one tick, measured on one contributed file and not derivable from the code as stated, since a load installs a save's clock whatever its value. The peer then checked the first against the five fixtures in the tree and found all five off the grid, which falsified its form and sharpened it: the derivable statement is that between installs every delta is a multiple of the step, so a run holds one residue class modulo the step. Re-derived here on the same five fixtures, one residue each, and added to the timing Subsystem as a contract and a replay oracle. The fade property stays out as one file's measurement; its useful form, if a second file agrees, is an oracle rather than a property.

**Update to the report above.** The relaying session's own re-measurement over the whole contributed file, tree not stated, has the clock pinned throughout, every tick value a multiple of the step, so the excursion at the divergence is minted steps from a pumped wait, 70 recorded against 224 replayed, and not elapsed real time; the arm is before tick 0 and what happens at the divergence is a load. The Format's arming-tick row therefore stands as scoped. Still a report: the file is outside the tree and no fixture exists.

## 2026-09-09

**Reported, not re-derived.** A concurrent session relays a third session's replay of a contributed session of about 90,000 ticks, stated as run on 9f3750f5: ticks 0 to 49 reproduce, and at tick 50 the recording's game clock steps back 1024 ms to the header's baseline while the replay, armed before tick 0, takes no such step and is 2464 ms ahead in the actor anchors by then. A fourth session reports the same file replaying to 2703 ticks on a tree with #663 and #664 stacked before they merged, and notes that on any tree before #663 the in-game menu ended every run at about tick 50, so on such a tree the place the run ended and the place it diverged were not separable. Neither figure has been re-measured on current main, and the two accounts do not agree on which tree the first was taken on. The file is outside the tree, so the number cannot be re-derived here. The mechanism relayed with it, that arming zeroes `TimerRefHR` and the reload then installs the snapshot's clock, is the order `record_reload_issue` takes; but on the console path `record_begin` runs after that reload lands, so the step it describes precedes the recording's first tick, and how it comes to sit at tick 50 of the file is not settled here. Whether the clock step and the consistency failure at that tick are one event is open too: the step is 1024 ms and the anchors are 2464 ms apart, which are different quantities, and a re-seed that both ends reconcile does not obviously leave a lasting divergence behind. The relaying session withdrew the mechanism on both counts. The `.rec` Format's row for `mode.step_armed_tick` is scoped to what #665 measured, a 98-tick fixture, and no longer says the window is inert. The same session withdrew an earlier attribution of that step to `RestoreTimer`; no concept carried it, checked. Its own note on the episode is worth keeping: a long document held the claim and its refutation ninety lines apart for a day, one of them labelled a withdrawal, which is the case for concepts being small and typed rather than documents being thorough.

**Fourth slice, the pinned clock's minting.** Distilled from docs/plan/ENGINE_TICK_POLICY_SURVEY.md, the measured step A of the engine-render ladder, with every claim re-checked against 9f3750f5: the present-site count is still 142, the eight pumped waits are where the survey put them, and the fixed-step tests carry the names cited. Two Decisions: the four mint policies stay four until the loop bodies own their step, with the measured cost of a present per modal surface and the two shapes where a present is not a tick; and the two pumps, polled and unpolled, with the stall that split them. Two Quirks: the two modals whose only clock is their own present, which is what kills "presents never mint"; and the eight waits that mint their own step through a call that looks like a no-op. The timing Subsystem gains the count and the rule that a step of clock is not a simulation tick; the recorder Subsystem's command row now says a command replays from the point in the frame it was recorded at, and its negative space gains the loop that reaches neither seam. What the survey holds beyond this is method and plan: the control that has to be unset rather than empty belongs to BUG_HUNTING.md's family, and the ladder's steps B to E are still a plan. One worked instance came from a concurrent session and was re-derived here before it went in: `PlayAcf`'s frame loop mints a polled step per frame since the video fix, and its six callers bracket it four against two, so which cinematic is playing decides whether the minted clock is kept.

**Update.** Rebased onto 9f3750f5, where #668 corrected docs/TIMING.md: the direct-writers table, the clock hook in the quoted body, the restore's lock scope, routine-name citations throughout, and the removed workaround recorded as removed. The timing Subsystem's row about the reference doc is retired, its writer list now defers to the doc's table rather than duplicating it, and the RestoreTimer Quirk cites the doc's row. The two findings below stay as the record of what was wrong and how it was found. First instance of a reference doc catching up with the bundle rather than the other way round.

**Third slice, engine timing.** Four facts surfaced by a concurrent recorder session and verified against d9cf303d before being taken, all about the timer rather than the recorder: a replay is the only instrument that compares two runs of the clock, so the recorder found them, but filing them under it would repeat the one-mechanism-several-manifestations error this log already names. The first subsystem slice pulled in a second subsystem's facts, and the profile had no way to say so; now it does, by the second subsystem existing. `/subsystems/timing.md` is distilled from docs/TIMING.md, whose headings already had the shape. Three Quirks: `SaveTimer` counts up at any depth while `RestoreTimer` no-ops at zero, which is the mechanism behind three separate fixes and the first instance of the `manifests` relation, pointed at it from the harness bracket Decision; `RestoreTimer` restores one of a coupled pair, so a rewind under a lock is not a rewind; and the two sample-fade loops in AMBIANCE.CPP end only because they read the wall clock, so the substitution the video fix made would wedge them without a pump. The recorder Subsystem gains its third seam class, a write the clock hook cannot see. SCHEMA.md now says what `asm_origin` names on original C++: where in what Adeline shipped the behaviour lives, which the ChangeCube quirk already assumed.

**Finding, line citations rot and nothing says so.** Between c2d1b2ba and d9cf303d, `git show <commit>:SOURCES/GAMEMENU.CPP | grep -n 'PlayAcf("'` moves from 3922, 4386, 5323 to 3984, 4448, 5385, while the three other call sites (GERETRAK.CPP 233, GERELIFE.CPP 1787, PERSO.CPP 816) do not move. Half of six line citations rotted in two days with every enclosing routine unmoved, and a reader spot-checking one row lands on a correct one two times in three. Measured by the concurrent session and reproduced here from origin alone; it is the evidence behind the schema's rule that `asm_origin` is a file and a routine.

**Finding, the reference doc on the timer.** docs/TIMING.md says `ManageTime` is the only function that mutates `TimerRefHR` and lists a body without the clock hook. At d9cf303d eight sites assign `TimerRefHR` directly: `RestoreTimer`, `SetTimerHR` and `Timer_EnableFixedDt` in the timer, two in `MainLoop`, two in the credits, one at the start of the attract reel; `LastTime` has three writers outside `ManageTime`; and the hook has been in the body since the recorder landed. The page that should predict the recorder's blind spot asserted the belief that makes it unreachable. The timing Subsystem records advance against install instead of repeating the claim, and its first draft here said "three", which was the timer alone. A doc fix is on a concurrent branch, for a change of its own.

**Finding, a whole citation layer rotted unnoticed.** Every line citation in docs/TIMING.md is wrong at d9cf303d, five for five: `MUSIC.CPP:312` and `PERSO.CPP:354` are blank lines, `OBJECT.CPP:5400` is an unrelated `snprintf`, `PERSO.CPP:1916` is a fragment of a camera comment, and `TIMER.H:57` misses `ManageSystem` by 110 lines. One number hid a stale claim rather than a stale line: the page says the `FollowCamTerrainFrame` workaround is "removable in a follow-up", and the follow-up landed as 002d5af2, so the symbol has not existed in SOURCES for months and a reader would go looking for it. Nothing checks a line number, and `make docs-symbols` cannot see a backticked path-and-line, so converting the citations to routine names is also what brings the document under the check for the first time. Re-derived here from origin. Stronger evidence for the `asm_origin` rule than the call-site census: that showed numbers rot, this shows a document whose whole subject is which call site does what to the clock had its entire citation layer rot without anyone noticing.

**Update.** Rebased onto d9cf303d after #665 and #666; `as_of` on the recorder Subsystem and Format moved with it. #665 carries the tick the deterministic step was armed on as `mode.step_armed_tick`, in the ignore list because a pair that armed on different ticks was measured to reproduce: the Format's header table gains the line, its negative-space row for `mode.fixed_dt` no longer claims the format cannot say when, and a row for the new line says what it does not do, which is align the two runs. The movement arm is no longer the only comparison across mismatched arming, and the Subsystem says so. #666 stops a from-boot recording carrying a savegame whose scene is -1: the Format's layout row and the one-file Decision say a session with no cube live carries no start snapshot and declares `-`, and the header table gains `setup.cube`, which existed before this base and was missing from the table.

**Update.** The bundle's charter landed as `/subsystems/knowledge.md`, from the maintainer's draft, and SCHEMA.md was reconciled with it. In the schema: `scope` required on Quirk; `# What it does not tell you` required on Subsystem, Format and Quirk; the `manifests` relation; `as_of` defined as transitional; the equivalence vocabulary fixed to the progress doc's legend, with a table of what the body must add and the OPEN note closed; `asm_origin` as file and routine; the generator named as the writer of generated concepts and the projection source corrected; body links required to resolve; lint rules 4, 7 and 10 reworded and 11 added; the schema's own `resource` supplied. In the charter, against the draft: the concept ID includes `.md`; body links resolve rather than dangle; the porting oracle names both files; the generator seam names the script, since a model cannot meet the reproducibility contract; the ownership seam says there is no ownership map; the Graphify seam says the graph is untracked; session-derived findings enter through the log; the Quirk open question carries the slice's evidence. `generated` on both files names the agent because the last meaningful change was the agent's, at the maintainer's request; the drafts are his and git carries them. The two recorder concepts were trimmed under the first principle: the `.rec` Format no longer transcribes the record layout RECORD.CPP's comment owns, and the recorder Subsystem's seams keep the judgment and drop the file table a grep answers.

**Update.** Rebased onto cc01c7ae after #663 and #664 merged; `as_of` on the recorder Subsystem and Format moved with it. Two rows changed on review from a concurrent recorder session, and both are the scope problem again. "The pinned step is a masking mechanism" covered two things: the structural bypass in `Record_ClockHook`, which is masking and is now cited from the code, and the alignment of where two runs arm the step, which removes an asymmetry rather than hiding a defect. Stated as "pinning masks", the next reader pins a comparison arm to make it green, and the movement arm is the counterexample the suite already carries. "A loose recording reproduces at 0 ms drift" covered two configurations, the console verb that arms the step for itself and a command-line `--record` that is host-sampled throughout, and the suite deliberately asserts only termination on the second. The Subsystem now carries both as separate rows, read at `as_of`. The Verdict row records that a harness replay survives the in-game menu since #663.

**Creation.** The bundle, with SCHEMA.md as the house profile over OKF v0.2, this log, the index, and the two slices the schema asks for before any bulk migration. Breadth: the recorder as a Subsystem, the `.rec` Format, three Decisions and one Quirk, distilled from docs/plan/RECORDER_OBSERVER_REVIEW.md, docs/plan/DIGEST_MEMBERSHIP.md, docs/plan/RECORDING_RESEARCH.md and docs/RECORDING.md, at commit c7b579b2. Depth: the `MulMatrixF` Quirk, the Porting Status projection for LIB386/3D it points into, and the generator behind that projection.

**Finding, the equivalence vocabulary.** ASM_TO_CPP_REFERENCE.md carries no per-routine status: it is a per-module table of what is built as C++, plus the mistranslation classes. The only status legend in the tree is ASM_VALIDATION_PROGRESS.md's `untested`, `tested`, `partial`, and this slice uses those three words. They cannot say "checked against the disassembly", and they cannot split "read from the C++" from "checked against nothing": both land on `untested`, so every `untested` concept says in its body which it is. The OPEN note in SCHEMA.md stays open.

**Finding, the projection source.** Porting Status is projected from ASM_VALIDATION_PROGRESS.md rather than ASM_TO_CPP_REFERENCE.md, because the reference file has no LIB386 rows and the per-routine evidence lives in the progress doc. The generator is `scripts/dev/knowledge_porting.py`, actor `process:knowledge-porting`. The `process:wiki-writer` subagent the schema names does not exist yet, and the banner names the process that produced the file.

**Finding, relation targets.** `ports` and `verified_against` take bundle-relative paths, and an ASM routine or a test file is not a concept. This slice points `ports` at the routine's section anchor in the generated Porting Status concept, `/porting/3d.md#mulmatrixf`, and wraps the test suites as Reference concepts for `verified_against`. Rule 10 then reads as: the anchored row's status and the Quirk's `equivalence` agree.

**Finding, links and CI.** lychee refuses a root-relative link unless it is given a root directory, so `lychee.toml` sets `root_dir = "docs/knowledge"`. The consequence is that the repository's link gate does not tolerate a dangling body link inside the bundle, although rule 4 says the profile does. This slice has none. To decide: keep the gate and drop the tolerance from the schema, or exclude the bundle from lychee and let the bundle lint own body links.

**Finding, missing anchors.** `docs/charters/` does not exist, so `subsystem` is a free string for now (`recording`, `3d`). Save format v37 does not exist either: `NUM_VERSION` is 36 and v37 is a named target in SAVEGAME.md, so the depth slice took a routine. No retro process is written down anywhere in the tree; the distillation input was the plan and review docs named above.

**Finding, the vocabulary on new construction.** The `.rec` Format sits at `tested` on the strength of host and automation tests, which is the second evidence level, so the vocabulary holds for code with no ASM original. SOURCES code is the original C++, so for a Quirk there the top evidence level is empty by construction; ChangeCube's evidence is measurement, and the body says so.

**Finding, the schema against its own rules.** SCHEMA.md declares `type: Reference` and carries no `resource`, which its own catalogue requires, so rule 2 and rule 3 pass on it and the per-type check does not. Rule 7 as written trips on SCHEMA.md itself, which spells the double-bracket wikilink syntax out in a code span in order to forbid it; the lint has to skip code spans, or the schema. The `asm_origin` example is a line-number citation, the form AGENTS.md says rots on the next edit and nothing checks; both Quirks here write it as file and routine instead, which is greppable and is what a reader needs to land on the act. All three are for the maintainer to settle, and none is changed here.

**Finding, a Quirk needs a scope.** Reported from a concurrent recorder session. The ChangeCube quirk holds on the loose-clock `--load` path; under `--fixed-dt` the seed is 0 and deterministic, and a code comment about that configuration reads as contradicting the quirk until the two scopes are stated. This engine forks behaviour on `--load` against fresh start, pinned against host clock, and headless against windowed, so an unscoped Quirk is a wrong belief waiting for a reader to resolve the contradiction by deleting one side. Both Quirks in this slice carry a producer key `scope`; the profile should require it.

**Finding, negative space.** Same source. Three format errors in one session were each an assumption that a header field says more than it does: `engine=` is not a build identity, `mode.fixed_dt` says that the step was pinned and not when, and a torn poll stream is not detectable the way a torn chunk is. A Format that records every guarantee and none of the traps is worse than the source comments it summarises. The `.rec` Format carries a "What it does not tell you" section; the profile should make it a required body section on Format, and probably on Subsystem.

**Finding, provenance on a moving subsystem.** Same source. The recorder changed in the day this slice was written (#661 merged, two branches unpushed, a `--replay` crash being fixed), so a Subsystem or Format concept about it is current only at a commit. Both carry a producer key `as_of` naming the commit their details were read from. The profile should require it on concepts about subsystems under active change, and should say which claims in a concept are structural and which are current, since a concept that mixes them without dating is worse than grep, which at least dates itself by what is in the tree.

**Finding, one mechanism, several manifestations.** Same source. One fact (`RestoreTimer` rewinds `TimerRefHR` and not `LastTime`, so restoring one of a coupled pair is not a rewind) was rediscovered three times in one night as a proposed mechanism, a dismissed hazard and the actual blocker, by two people who had both written it down. A typed relation for "same mechanism, different manifestation" would have caught it; no instance exists in this slice, so the key is proposed here and not defined.
